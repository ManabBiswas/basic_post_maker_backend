<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Poképiece</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body class="bg-gradient-to-br from-emerald-50 via-white to-green-100 min-h-screen font-sans text-gray-800">
    <main class="max-w-3xl mx-auto px-4 sm:px-6 py-12">
      <section class="bg-white/60 backdrop-blur-lg border border-emerald-100 rounded-2xl shadow-xl p-8 space-y-6">
        <h1 class="text-2xl font-bold text-emerald-800 text-center">Edit Your Poképiece 🛠️</h1>

        <form method="POST" action="/update/<%= post._id %>" class="space-y-6">
          <!-- Title Field -->
          <div>
            <label for="title" class="block text-sm font-medium text-emerald-700 mb-1">Title</label>
            <input
              type="text"
              id="title"
              name="title"
              value="<%= post.title %>"
              class="w-full px-4 py-2 bg-green-50 border border-emerald-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-emerald-400 transition"
              placeholder="Update your adventure title"
            />
          </div>

          <!-- Content Field -->
          <div>
            <label for="content" class="block text-sm font-medium text-emerald-700 mb-1">Story</label>
            <textarea
              id="content"
              name="content"
              rows="6"
              class="w-full px-4 py-2 bg-green-50 border border-emerald-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-emerald-400 transition resize-none"
              placeholder="Revise your Pokéstory..."
            ><%= post.content %></textarea>
          </div>

          <!-- Submit Button -->
          <div class="text-center">
            <input
              type="submit"
              value="Save Changes"
              class="px-6 py-3 bg-gradient-to-r from-emerald-500 to-green-400 text-white font-semibold rounded-xl hover:brightness-110 hover:shadow-xl transition duration-300"
            />
          </div>
        </form>
      </section>
    </main>
  </body>
</html>