<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>pokepost — Update Profile</title>
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>

<body class="bg-gradient-to-br from-emerald-50 via-white to-green-100 min-h-screen font-sans text-gray-800">
  <!-- Header -->
  <header class="sticky top-0 z-10 flex justify-between items-center px-6 py-4 bg-white/70 backdrop-blur-md shadow-sm border-b border-green-100">
    <h1 class="text-xl sm:text-2xl font-bold text-emerald-700">Update Profile 🍃</h1>
    <a href="/" class="text-sm font-medium text-blue-500 hover:text-blue-600 transition">← Back to Home</a>
  </header>

  <!-- Update Form -->
  <main class="max-w-xl mx-auto px-4 sm:px-6 py-10">
    <section class="bg-white/70 backdrop-blur-md border border-emerald-100 rounded-2xl shadow-lg p-6">
      <h2 class="text-xl font-bold text-emerald-700 text-center mb-6">Edit Your Info</h2>

      <form method="post" autocomplete="off" action="/update" enctype="multipart/form-data" class="space-y-6">
        <!-- Profile Image Upload -->
        <div>
          <label class="block text-sm font-medium text-gray-700 mb-2">Profile Image</label>
          <input type="file" name="image" accept="image/*"
            class="w-full px-4 py-2 bg-green-50/80 border border-emerald-200 rounded-lg focus:ring-2 focus:ring-emerald-400 focus:border-emerald-400 transition" />
        </div>

        <!-- About Textarea -->
        <div>
          <label class="block text-sm font-medium text-gray-700 mb-2">About You</label>
          <textarea name="about" rows="4" placeholder="Write a bit about your journey..."
            class="w-full px-4 py-3 bg-green-50/80 border border-emerald-200 rounded-lg focus:ring-2 focus:ring-emerald-400 focus:border-emerald-400 transition resize-none"></textarea>
        </div>

        <!-- Submit Button -->
        <button type="submit"
          class="w-full bg-gradient-to-r from-emerald-500 to-green-400 text-white py-3 rounded-xl font-semibold hover:brightness-110 shadow-md hover:shadow-xl transition duration-300 flex items-center justify-center gap-2">
          <span>💾</span>
          Save Changes
        </button>
      </form>
    </section>
  </main>
</body>
</html>